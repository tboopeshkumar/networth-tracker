# Copy to .env.local for local development (never committed).
# For the live site, set the same names under GitHub → Settings →
# Secrets and variables → Actions → Variables.
#
# Everything here ends up inside the published JavaScript, so treat it all as
# public. The Google values are public by design. The email lists are visible
# to anyone who opens the site — they only decide what the UI offers, never
# who can reach your data. Google enforces that (see README).

VITE_GOOGLE_CLIENT_ID=1234567890-abc123.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=AIza...
VITE_GOOGLE_APP_ID=1234567890

# Who may use the app. Leave empty to rely only on Google's test-user list.
VITE_ALLOWED_EMAILS=
# Who sees Edit and Add. Leave empty to let every allowed account edit.
# Not a security control: share the sheet as Viewer to actually block writes.
VITE_EDITOR_EMAILS=

VITE_IDLE_MINUTES=20
